'''
              Абстрактные классы
Абстрактный класс в Python — это класс, который не может быть использован для создания объектов напрямую. Его основная цель — служить базой для других классов. Абстрактные классы обычно используются для задания общего интерфейса или структуры, которую должны реализовать все его наследники.

В Python абстрактные классы создаются с помощью модуля abc (Abstract Base Classes).

1. Как создать абстрактный класс:
Чтобы создать абстрактный класс, нужно использовать декоратор @abstractmethod для методов, которые должны быть реализованы в дочерних классах.

Пример использования:'''
from abc import ABC, abstractmethod

class Animal(ABC):
    @abstractmethod
    def sound(self):
        pass

class Dog(Animal):
    def sound(self):
        return "Woof!"

# Невозможно создать объект абстрактного класса
# animal = Animal()  # Это вызовет ошибку

dog = Dog()
print(dog.sound())  # Woof!
'''Особенности абстрактных классов:
Абстрактный класс может содержать как абстрактные методы (без реализации), так и обычные методы.
Он не может быть инстанцирован (то есть, из него нельзя создавать объекты напрямую).
Классы, наследующие абстрактный класс, должны реализовать все его абстрактные методы.

Абстрактные классы: когда нужно задать общий интерфейс для группы классов, но оставить детали реализации для дочерних классов.
'''
